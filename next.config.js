/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
        "staatsbibliothek-berlin.de",
        "raw.githubusercontent.com", // add any other domains you use
        ],
  },}

module.exports = nextConfig
